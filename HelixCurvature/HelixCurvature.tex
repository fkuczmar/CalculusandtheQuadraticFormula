\documentclass{ximera}
\title{Maximizing the Curvature of a Cylindrical Helix}

\newcommand{\pskip}{\vskip 0.1 in}

%\newtheorem{theorem19}{Robinson's arc length Theorem for Prolate Cycloids, Local Version}
%\newtheorem{theorem20}{An arc length Theorem for Prolate Epi and Hypocycloids}
%\newtheorem{theorem21}{Robinson's Lemma}
%\newtheorem{theorem22}{The Inclination Lemma}
%\newtheorem{theorem23}{An arc length Theorem for General Cycloids}

\begin{document}
\begin{abstract}
\end{abstract}
\maketitle
 

It seems intuitive that the curvature of the cylindrical helix
\[
       {\bf p}(t) = (a \cos\theta, a \sin\theta, b\theta) \, \, \theta\in \mathbb{R}, a,b\geq 0,
\]
decreases if $a$ increases (holding $b$ fixed) and decreases if $b$ increases (holding $a$ fixed). The latter is true, but as Michael Vincent noticed, the former is not. With the height $b$ of the helix at rotation angle $\theta=1$ held constant, the curvature is a maximum for $a=b$. Here's one explanation.

With $b$ held constant, the helix's radius of curvature  as a function  $\rho=f(\phi)$ of its inclination angle is is symmetric about $\phi=\pi/4$. To see why geometrically, we represent a one-radian arc ($\Delta \theta=1$) of a helix with pitch angle $\phi_1$ as segment $AC$ with length $s_1 = b\csc\phi_1$ in the figure below. Wrapping the segment around a cyclinder of radius $a=b\cot\phi_1$ gives the helical arc. The key now is to add the radius of curvature to the figure.


\begin{onlineOnly}
    \begin{center}
\desmos{f2sragdapj}{900}{600}
\end{center}
\end{onlineOnly}

\href{https://www.desmos.com/calculator/f2sragdapj}{Helix Maximum Curvature}

Because the curvature is constant, the radius of curvature is the average rate of change $s_1 / \Delta \alpha_1$, where $\alpha_1$ is the angle through which the unit tangent ${\bf T}$ to the helix between points $A$ and $C$. Since the tangent makes the angle $\phi_1$ with the $xy$-plane, its hodographis lies along the circle of latitude with  (dimensionless) radius $r = \cos\phi_1$ on the unit sphere. Since between $A$ and $C$, $\Delta\theta= 1$, we have $\Delta \alpha_1 = r \Delta \theta = \cos\phi_1$. So the helix has radius of curvature $\rho_1 = a\sec\phi_1$, equal to the hypotenuse $AB$ of right triangle $\Delta ACB$.  As the figure shows, this implies the helix with pitch angle $\phi_2 = \pi/2 - \phi_1$ has the same radius of curvature $\rho_2 = AB$.

So th radius of curvature function $\rho = f(\phi)$ is symmetric about $\phi=\pi/4$ as claimed. To show it attains a maximum there amouts to showing that of all right triangles with an altitude of length $b$, the one with the shortest hypotenuse is isoceles. Or equivalently of showing that of all right triangles with a given hypotenuse $AB$, the one with the longest altitude to the hypotenuse is isosceles. The latter is true since all such right triangles are inscribable in a semi-circle with diameter $AC$. 


The worksheet below shows complementary helices with the same height $b$ and equal curvatures. Drag the sliders $b$ and $\phi_1$ to change the height and pitch angles.

\begin{onlineOnly}
    \begin{center}
\desmosThreeD{xq8nuuxbcb}{900}{600}
\end{center}
\end{onlineOnly}

\href{https://www.desmos.com/3d/xq8nuuxbcb}{Helix  Curvature}




\end{document}
